CREATE OR REPLACE add_passenger(train_id INTEGER, dated DATE, coach_type VARCHAR, seats_needed INTEGER, passenger_name VARCHAR, )
    AS
    $$
    DECLARE
        
    BEGIN

    END
    $$
LANGUAGE plpgsql;